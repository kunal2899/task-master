const VALIDATION_ERRORS = {
  DUPLICATE_EMAIL: 'duplicate key value violates unique constraint "users_email_key"',
  INVALID_PAYLOAD: 'Invalid payload provided',
};

module.exports = { VALIDATION_ERRORS };
